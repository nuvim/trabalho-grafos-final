# trabalho de pcv (grafos)

início do trabalho final de Algoritmos em Grafos (problema do caixeiro viajante).

o que foi feito até o momento:
- leitura dos arquivos `tsp` direto pelo terminal (stdin) pra bater com a correção.
- puxando só oq importa (nodes_coords).
- calculo de distância euclidiana (`EUC_2D`) cravado com a fórmula de `floor` pedida nas instruções.
- montagem de uma matriz de adjacência pra otimizar acesso aos dados na hora que o teste dos algoritmos de achar rota (pra poupar tempo e nao estourar limite).

próximo passo é colocar a heurística inicial pra rodar pq no momento ele só lê mas não da saida.
